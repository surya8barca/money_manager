const connectionString = "Server=.;Database=Money_Analyst;Trusted_connection=Yes;Driver={SQL Server}";
const port = 8080;

module.exports={
    connectionString:connectionString,
    port:port
}